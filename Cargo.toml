# `hap` talks HomeKit to the thermostat on the LAN — local, works when the internet is down,
# and needs no developer account.
#
# There was a second package, `cloud`, against api.ecobee.com. It shared nothing with this one
# but a vendor, and it was the only driver here that stopped working when the internet did.
# Dropped rather than carried; remote access went with it.
[workspace]
resolver = "3"
members = ["hap"]

# A driver is a dylib the controller loads; a debug-sized one is several times larger to ship.
[profile.release]
opt-level = 2
codegen-units = 16
